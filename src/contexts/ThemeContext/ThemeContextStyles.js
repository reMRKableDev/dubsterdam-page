import styled from "styled-components";
import { bgColor, textColor } from "../../utils/themeColors";

export const ThemeWrapper = styled.div`
  background-color: ${bgColor};
  color: ${textColor};
`;
